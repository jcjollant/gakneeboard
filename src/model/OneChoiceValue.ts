export class OneChoiceValue {
    label: string
    value: number
    title: string
    constructor(label:string, value:number, title:string) {
        this.label = label
        this.value = value
        this.title = title
    }
}