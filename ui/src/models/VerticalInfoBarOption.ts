export enum VerticalInfoBarOption {
  all = 'all',
  version = 'ver',
  hide = 'hide',
}

export enum VerticalInfoBarContent {
  Version = 'version',
  PageName = 'name',
  Tail = 'tail',
  Date = 'date'
}
