# Cypress Testing
## Command line
Open the UI
`npx cypress open`
Run the test suite:
`npx cypress run`

Try / Debug tests
`npx cypress open`

Run Cypress tests 
`npx cypress run --browser chrome`

## Print Checks
* Load Checklist Demo
 * Two Pages + Normal + Landcape
   * Check Margins and Scale are default
   * Check on Color and Monochrome
   * Dont' print -> exist print mode
 * Two Pages + Normal + Portrait
 * Two Pages + Flipped + Landcape
 * One Page + Normal + Portrait
 * One Page + Flipped + Portrait
* Load Default Checklist
