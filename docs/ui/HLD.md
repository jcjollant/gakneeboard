# FTUX
First time user can arrive via the landing page (./public/rwy01.html) or directly on /index.html
In both cases, we check localstore to confirm they never showed up and use the following sequence
rwy01.html > Index.html/ftux > index.html
FTUX will show the user with a simpler experience with only 4 demos.